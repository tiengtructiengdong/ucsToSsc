#include <iostream>
#include <sstream>
#include <vector>

std::string join(const std::vector<std::string>& vct, std::string c);
std::string toSsc(std::string stepLine);
int gcd(int a, int b);
int lcm(int a, int b);

